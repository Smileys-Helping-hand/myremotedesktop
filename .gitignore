node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# TypeScript incremental build state
*.tsbuildinfo

# Rust build output and packaged installers
src-tauri/target/

# Generated by the Tauri CLI from tauri.conf.json and capabilities/
src-tauri/gen/schemas/

# Locally built installers
installers/

# Updater signing keys. The private key must never enter the repository — it
# lives in ~/.remotedesk-keys and in the TAURI_SIGNING_PRIVATE_KEY CI secret.
*.key
!**/*.key.pub
